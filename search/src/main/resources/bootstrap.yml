server:
    port: 8086
    session:
      cookie:
        name: search-service
    context-path: /search

spring:
  application:
    name: search-service

eureka:
  client:
    region: default
    registry-fetch-interval-seconds: 10
    service-url:
      defaultZone: http://localhost:8762/eureka/

